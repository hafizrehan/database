@model IEnumerable<MvcApplication.Models.Student1>

@{
    ViewBag.Title = "searchallstd";
    Layout = "~/Views/Shared/_student.cshtml";
}

<script type="text/javascript">
    $(document).ready(function () {
        (function () {
            $("#datepicker").datepicker();
        });
    });
</script>

<form method="post" action="~/Home/teacher">
 <input type="submit" value="Back" class="btn btn-primary pull-center"/>
</form>



<form method="post" action="/Teacher/chkattendance">


<h2 class="title">Mark Attendance</h2> <input type="date" id="datepicker" name="date" class="btn btn-primary pull-center"/> 

<table border="1">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name) 
        </th>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.Attendance)
            
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
          </td><td></td>       <td>
          <center>
          <input type="checkbox" name="n" value="P" />
          <input type="text" value="@Html.DisplayFor(modelItem => item.Name)" name="tx" hidden="hidden"/>
          </center>
        </td>
        
    </tr>
}

</table>
    &nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp &nbsp<input type="submit" value="submit" class="btn btn-primary pull-center"/>
    </form>